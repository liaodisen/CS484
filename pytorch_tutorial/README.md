# PyTorch Tutorial

## Requirements

**Python 3.10+** recommended.

Install dependencies:

```bash
pip install torch torchvision numpy pillow scikit-learn matplotlib
```

Or using uv:

```bash
uv pip install torch torchvision numpy pillow scikit-learn matplotlib
```
